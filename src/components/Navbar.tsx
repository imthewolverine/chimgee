'use client';

import React from 'react';

const Navbar: React.FC = () => {
    return (
        <nav className="">
            <div className="">
                Logo
            </div>
        </nav>
    );
};

export default Navbar;